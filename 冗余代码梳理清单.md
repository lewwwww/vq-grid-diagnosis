# 毕设项目-冗余/无用代码梳理清单

> 检测方式：全量文件清单 → 逐文件引用追踪（import / 标签 / API 路径 / 依赖引用），
> 排除 node_modules、.nuxt、.output 构建产物。判断标准是"真实引用"而非文件名猜测。
> 检测日期：2026-09-02

---

## 一、前端死代码（无任何引用，可安全删除）

### 1. composables —— 4 个文件全部零调用

| 文件 | 证据 |
|---|---|
| `composables/useChart.ts` | 全项目仅自身文件出现 |
| `composables/useForm.ts` | 全项目仅自身文件出现 |
| `composables/useTable.ts` | 全项目仅自身文件出现 |
| `composables/useRealtimeData.ts` | 全项目仅自身文件出现（内部含 WebSocket 代码，但无人调用） |

> 说明：4 个"可复用组合式函数"实际没有页面/组件使用（页面里全是自己写的逻辑）。
> Nuxt 会自动加载 composables 目录，但没人调用就是死代码。

### 2. utils 目录

| 文件 | 证据 | 处理 |
|---|---|---|
| `utils/request.ts` | **全项目零引用**（axios 封装，无人 import、无 request.get/post 调用） | 可整文件删除 |
| `utils/export.ts` 中 `exportJSON` | 零引用 | 只保留 `exportExcel`（被 analysis 页面使用），其余可删 |
| `utils/export.ts` 中 `exportCSV` | 零引用 | 同上 |
| `utils/export.ts` 中 `exportDiagnosisReport` | 零引用，且字段是旧版接口（`device_id`/`node_ids`/`risk_level`），与当前 `deviceId`/`nodeIds`/`faultType` 不匹配 | 明显遗留死代码 |

### 3. stores/user.ts 内权限相关（部分死代码）

| 内容 | 证据 |
|---|---|
| `permissions` state | 无任何页面引用 `.permissions` |
| `getPermissions()` action | 零调用，且请求的 `/api/auth/permissions` **后端接口不存在**（悬空调用） |
| `hasPermission()` | 零调用 |

> 保留：`login/register/logout/getUserInfo`、`isLogin/isOperator/isAdmin`（均被使用）。

---

## 二、前端死依赖（package.json 里装了但代码未使用）

| 依赖 | 证据 |
|---|---|
| `axios` | 仅被死代码 `utils/request.ts` import，删除 request.ts 后无引用 |
| `echarts-gl` | 全前端零 import（dashboard/analysis 用的是 `echarts` 本体） |
| `csv-parse` | server 端**自研手写 parseCSV**（注释明确"不依赖外部库"），从未 import csv-parse |
| `nodemailer` | 代码零引用；.env 有 SMTP 配置但邮件功能**未实现**（notifications.ts 只做系统通知入库） |

> `xlsx` 保留：前端 `exportExcel` 动态 `import('xlsx')` 且被 analysis 页面使用。
> `echarts` 保留：dashboard、analysis 直接使用。
> `ignore` 保留：package.json overrides 用于修复传递依赖冲突。

---

## 三、server 死接口（前端无任何调用）

| 接口文件 | 对应路由 | 说明 |
|---|---|---|
| `api/v1/device/[id].get.ts` | `GET /api/v1/device/{id}` | 前端设备详情来自 page 列表，未调用此接口 |
| `api/v1/diagnosis/[id].get.ts` | `GET /api/v1/diagnosis/{id}` | 前端只用 diagnosis/page、batch 等 |
| `api/v1/user/detail.get.ts` | `GET /api/v1/user/detail` | 前端用 user 列表/delete/update/create，未用 detail |
| `api/v1/training/optimize.post.ts` | `POST /api/v1/training/optimize` | 全项目（含 server 内部）无调用 |
| `api/v1/training/generate-sample.post.ts` | `POST /api/v1/training/generate-sample` | 全项目无调用 |
| `api/v1/training/jobs/[id].get.ts` | `GET /api/v1/training/jobs/{id}` | 前端只用 jobs 列表与 stop |
| `ws/realtime.ts` | WebSocket `/api/ws` | 前端唯一连 ws 的 useRealtimeData 是死代码 → **无人连接** |

> 说明：以上均为"前端无调用"，若你希望保留作为预留接口（如 Postman 手动调试、后续扩展），可保留但应知晓它们当前未被使用。

---

## 四、算法层冗余

| 文件 | 状态 |
|---|---|
| `algorithm/model_inference_service.py.bak`（14KB） | 重构遗留备份，可删（当前版本已生效） |
| `algorithm/train_6class_model.py.bak`（21KB） | 同上 |
| `algorithm/__pycache__/` | Python 缓存，可删（自动重建） |
| `algorithm/realistic_data_adapter.py`（11KB） | **无任何脚本 import**，虽有 `__main__` 但未接入主流程、README 未提及 → 孤儿模块，确认不需要后删 |
| `data_new/models/fault_6class_*`（10 个实验目录 + 一堆 .pth） | 实验残留产物（balanced/default/improved/optimized/seed123/seed456/weighted...）。**只有 `fault_6class/` 是推理服务实际加载的**，其余是消融/对比实验的权重，确认后归档或删除（占空间） |

> 保留：`print_results.py`、`visualize_optuna_results.py`（独立工具脚本，可手动运行看实验/优化结果）；
> `start_service.py`（README 官方启动入口）；`baseline_models.py`、`train_baseline_models.py`（run_all_experiments 引用，对比实验用）。

---

## 五、发现的真实 Bug（不是死代码，但建议修）

### 1. 停止训练接口路径不匹配（会 404）
- 前端 `pages/settings/index.vue:1007` 调用：`/api/v1/training/stop/${id}`
- 后端路由：`server/api/v1/training/jobs/[id]/stop.post.ts` → 实际路径 `/api/v1/training/jobs/{id}/stop`
- **前后路径不一致 → 前端点"停止训练"会请求 404，功能实际失效**

### 2. favicon 引用缺失
- `nuxt.config.ts` 引用 `/favicon.ico`，但 `public/` 只有 `logo.svg`，没有 favicon.ico → 浏览器请求 404（不影响功能，影响小图标显示）

---

## 六、清理优先级建议

| 优先级 | 内容 | 收益 |
|---|---|---|
| P0（安全） | 4 个 composable、utils/request.ts、export 死函数、user store 权限段、2 个 .bak、__pycache__ | 纯死代码，零风险 |
| P1 | server 7 个死接口、ws/realtime.ts | 减少维护面（若确认无扩展需求） |
| P2 | 依赖：axios / echarts-gl / csv-parse / nodemailer | 减小安装体积 |
| P3 | realistic_data_adapter、实验模型目录归档 | 需你确认是否还需要 |
| Bug | training stop 路径、favicon | 修复，非删除 |

> ⚠️ 处理前建议 git commit 当前状态（当前重构未提交），再逐个删，方便回滚。
